crontab -e
01,20 04,05 1-15 1,6 * /usr/bin/somedirectory/somecommand
01 and 20 min past the hours of 4:00am and 5:00am on the 1st through the 15th of every January and June.

minute (0-59), hour (0-23, 0 = midnight), day (1-31), month (1-12), weekday (0-6, 0 = Sunday), command


*/20 * * * * command
#means every 20 minutes starting from 0, do command

60 24 31 12 7 /usr/bin/somthing
