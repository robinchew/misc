crontab -e
01,59 0,23 1-31 10,12 * /usr/bin/somedirectory/somecommand
01 and 59 minutes past the hours of 0AM and 23PM on the 1st through to the 31st of every October and December.

minute (0-59), hour (0-23, 0 = midnight), day (1-31), month (1-12), weekday (0-6, 0 = Sunday), command


*/20 * * * * command
#means every 20 minutes starting from 0, do command

60 24 31 12 7 /usr/bin/somthing
