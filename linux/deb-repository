192.168.1.171 dns => as client for testing install from local repo
9. edit /etc/apt/sources.list
10. deb http://192.168.4.100/repo/ /
11. sudo apt-get update

# SCRIPT - update_repo
192.168.1.132 squid-proxy => as a local repo
0. create a folder like /home/hans/repository
1. cp --update --verbose /var/cache/apt/archives/*.deb /home/hans/repository
2. cd /home/hans/repository; 
   dpkg-scanpackages . /dev/null > Packages; 
   gzip -9c Packages > Packages.gz
Note : these 3 lines (1-3) are put in 1 shell script called update-repo
# END SCRIPT

4. edit /etc/apt/sources.list
5. deb file:/home/<username>/repository/
6. sudo apt-get update
7. sudo apt-get install apache2
8. sudo ln -s /home/<username>/repository/ /var/www/repo

To install new program for the first time:
@132-local repo
sudo apt-get -d install netbeans

run update-repo script
done on repo server

on client
apt-get update
apt-get install netbeans
