# START VM #
su - robin -c 'VBoxManage startvm "SVN Server" -type vrdp'

# CLONE VDI #
# source.vdi needs to be FULL PATH sometimes, if not in VDI folder
VBoxManage clonevdi source.vdi target.vdi
#after clone, vbox adds a new line to /dev/udev/rules.d/70-persistent-net.rules:
SUBSYSTEM=="net", ACTION=="add", DRIVERS=="?*", ATTR{address}=="00:30:1b:81:70:8e", ATTR{type}=="1", KERNEL=="eth*", NAME="eth1"
#so i made a vdi file without the line above with eth0, so that vbox will add theline above making the new eth0

OR

cp Master.vdi Clone.vdi
VBoxManage internalcommands setvdiuuid Clone.vdi

# BRIDGE NETWORKING #
# newer version of virtualbox is automatic, making below irrelevant

sudo aptitude install bridge-utils

#/etc/network/interfaces
auto lo
iface lo inet loopback

auto br0
iface br0 inet dhcp
        bridge_ports eth1
        bridge_fd 2.5

#setup application: gksudo .VirtualBox/setup
#!!NEED TO CHANGE USERNAME!!
#------------------------------------
#!/bin/bash

# Create an new TAP interface for the user 'robin' and remember its name.
interface=`VBoxTunctl -b -u robin` ## replace robin with your user

# If for some reason the interface could not be created, return 1 to
# tell this to VirtualBox.
if [ -z "$interface" ]; then
exit 1
fi

# Write the name of the interface to the standard output.
echo $interface

# Bring up the interface.
ifconfig $interface up

# And add it to the bridge.
brctl addif br0 $interface
#-------------------------------

#terminate: gksudo .VirtualBox/terminate
#--------------------------------
#!/bin/bash
# the interface name.
 brctl delif br0 $2
#
# # And use VBoxTunctl to remove the interface.
 VBoxTunctl -d $2
#------------------------------------
#
/etc/udev/rules.d/20-names.rules 
KERNEL=="tun", NAME="net/%k" 
to
KERNEL=="tun", NAME="net/%k", GROUP="vboxusers", MODE="0660"
